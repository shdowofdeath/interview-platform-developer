{
  "setupTasks": [
    {
      "name": "Bootstrap (dependencies, infrastructure, seed)",
      "command": "scripts/bootstrap.sh"
    }
  ],
  "tasks": {
    "api": {
      "name": "API (:9400)",
      "command": "cd services/ingest && uv run uvicorn app:app --host 0.0.0.0 --port 9400 --reload",
      "runAtStart": true,
      "preview": { "port": 9400 }
    },
    "worker": {
      "name": "Temporal worker",
      "command": "cd services/ingest && uv run python worker.py",
      "runAtStart": true
    },
    "mock-upstream": {
      "name": "Mock upstream (:9401)",
      "command": "cd services/mock-upstream && uv run uvicorn app:app --host 0.0.0.0 --port 9401",
      "runAtStart": true
    },
    "verify": {
      "name": "Verify environment",
      "command": "scripts/verify_env.sh",
      "runAtStart": false
    },
    "infra": {
      "name": "Infrastructure status",
      "command": "docker compose ps",
      "runAtStart": false
    },
    "collector-logs": {
      "name": "Collector logs",
      "command": "docker compose logs -f otel-collector",
      "runAtStart": false
    },
    "seed": {
      "name": "Re-seed the database",
      "command": "cd services/ingest && uv run python scripts/seed.py",
      "runAtStart": false
    },
    "test": {
      "name": "Tests",
      "command": "cd services/ingest && uv run pytest -q",
      "runAtStart": false
    },
    "lint": {
      "name": "Ruff",
      "command": "cd services/ingest && uv run ruff check .",
      "runAtStart": false
    }
  }
}
